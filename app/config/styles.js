import { StyleSheet } from 'react-native';

export default new StyleSheet.create({
  listItem: {
    marginLeft:0,
    paddingLeft:20,
    marginTop:5,
  },
  list: {
    backgroundColor:'white'
  }
})
